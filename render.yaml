services:
  - type: web
    name: coup-server
    runtime: node
    plan: free
    # Render injects PORT; main.ts binds to it.
    buildCommand: npm install && npm run build:server
    startCommand: npm run start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
